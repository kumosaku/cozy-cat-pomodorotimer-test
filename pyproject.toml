[project]
name = "cozy-cat-pomodorotimer"
version = "0.1.0"
description = "Pomodoro timer with interactive blob cat"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.scripts]
serve = "python serve.py"
